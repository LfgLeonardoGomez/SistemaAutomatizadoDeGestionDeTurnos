## 1. Setup

- [x] 1.1 Create `backend/tests/test_flexible_auth.py`, seeding fixtures for: an active professional A with a known `api_key` and a valid JWT, a second active professional B with a different `api_key` and JWT (for the precedence scenario), and an inactive professional C with both a valid `api_key` and a valid JWT — mirror the seeding pattern in `test_recordatorio_router.py` (api-key) and `test_turnos_router.py` / `authenticated_client` (JWT).
- [x] 1.2 Run the full backend test suite once (`pytest backend/tests`) and record the baseline pass count before touching any production code — this is the Strict TDD safety net proving `turnos.py` and `dependencies.py` behavior is not broken by this change. Baseline: 468 passed, 5 pre-existing failures (unrelated: `test_notificacion_service.py`, `test_telegram_service.py`), 1 skipped, 1 xfailed, 3 xpassed.

## 2. RED — Failing tests for `get_current_profesional_flexible` (all 9 scenarios)

Target endpoint for all cases below: `GET /turnos/disponibles` (the design's designated unit-test surface), invoked via `TestClient`. Every test in this group MUST fail first because `get_current_profesional_flexible` / `FlexibleProfesionalDep` do not exist yet.

- [x] 2.1 Test: valid `X-API-Key` only, no `Authorization` header → 200, response resolves professional A (spec: "Authenticate via API key only").
- [x] 2.2 Test: valid `Authorization: Bearer <jwt>` only, no `X-API-Key` header → 200, response resolves professional A exactly as pre-change JWT behavior (spec: "Authenticate via JWT only (unchanged)").
- [x] 2.3 Test: both a valid `X-API-Key` (professional A) and a valid Bearer JWT (professional B) present → 200, resolved professional is **A** (X-API-Key precedence) (spec: "Both headers present and valid").
- [x] 2.4 Test: invalid/unknown `X-API-Key` present together with a valid Bearer JWT → 200, falls through and resolves via JWT (OR semantics) (spec: "X-API-Key present but invalid, valid JWT also present").
- [x] 2.5 Test: no auth headers at all → 401, generic `"Credenciales inválidas"` body (spec: "Neither credential valid", no-headers case).
- [x] 2.6 Test: invalid `X-API-Key` and no/invalid Bearer JWT → 401, same generic body as 2.5 — assert byte-identical `detail` across both failure shapes so no scheme is leaked (spec: "Neither credential valid", both-invalid case).
- [x] 2.7 Test: valid `X-API-Key` belonging to inactive professional C, no JWT → 401 (spec: "Inactive professional under either scheme", api-key branch).
- [x] 2.8 Test: valid Bearer JWT belonging to inactive professional C, no `X-API-Key` → 401 (spec: "Inactive professional under either scheme", jwt branch).
- [x] 2.9 Test (on a non-`/turnos/*` professional endpoint, e.g. `GET /pacientes/` or `GET /lista-espera/`): request carries only a valid `X-API-Key` header and no `Authorization` header → 401 (endpoint keeps its current JWT-only `CurrentProfesionalDep`, flexible dep is not wired here) (spec: "Non-turno endpoint is unaffected").
- [x] 2.10 Execute `pytest backend/tests/test_flexible_auth.py` and confirm all 9 tests above fail with an import/attribute error (RED confirmed) before writing any implementation code. **Deviation**: 6/9 failed on assertion (wrong status/body), not import/attribute error — `get_current_profesional_flexible` doesn't need to exist at import time since tests drive it via HTTP through `TestClient`, so absence surfaces as a behavioral mismatch, not a Python import error. The remaining 3 (2.2, 2.4, 2.8) passed even pre-implementation because they only exercise the pre-existing JWT-only path, which already satisfied that specific scenario by coincidence. RED intent (implementation incomplete, tests fail to reflect true target behavior) was still satisfied for the scenarios that actually required the new dependency.

## 3. GREEN — Implement `get_current_profesional_flexible`

- [x] 3.1 In `backend/app/dependencies.py`, add `get_current_profesional_flexible(x_api_key: str | None = Header(default=None, alias="X-API-Key"), authorization: str | None = Header(default=None), db: DbDep) -> Profesional`. Both headers must be declared with `default=None` (no `auto_error`, no required `Header`/`HTTPBearer` dependency) so absence of either does not raise before the OR-fallback runs.
- [x] 3.2 Implement precedence inside the function body: if `x_api_key` is present, look up `Profesional` by `api_key == x_api_key AND is_active == True` (same query shape as `get_profesional_by_api_key`); on hit, return it immediately without touching the `Authorization` header.
- [x] 3.3 If `x_api_key` is absent or does not resolve, and `authorization` is present, parse the `Bearer <token>` scheme manually, decode the JWT with the same `jwt.decode(... Settings().secret_key, algorithms=[Settings().algorithm])` call used in `get_current_profesional`, extract `sub`, and look up the active `Profesional` by id.
- [x] 3.4 If neither branch resolves a `Profesional` (missing headers, invalid api key, invalid/expired JWT, malformed bearer scheme, or inactive professional on either path), raise one shared `HTTPException(401, detail="Credenciales inválidas", headers={"WWW-Authenticate": "Bearer"})` — reuse exactly one exception instance/construction so the body is provably identical across all failure branches (do not build per-branch messages).
- [x] 3.5 Add `FlexibleProfesionalDep = Annotated[Profesional, Depends(get_current_profesional_flexible)]` alongside the existing `CurrentProfesionalDep` definition. Do not modify `get_current_profesional` or `get_profesional_by_api_key`.
- [x] 3.6 Run `pytest backend/tests/test_flexible_auth.py::<case>` incrementally per scenario (2.1 → 2.9), fixing the implementation until each turns GREEN. **Deviation**: implemented directly against all 7 `turnos.py` endpoints (task 4) in the same step instead of temporarily wiring only `/turnos/disponibles` first — the implementation was correct on the first pass (verified by running the full matrix immediately after), so the temporary-single-endpoint intermediate step was skipped as unnecessary churn. Scenario 2.9 stayed green against the unmodified `pacientes.py` dependency (no wiring there), as required.
- [x] 3.7 Confirm all 9 tests in `test_flexible_auth.py` pass (GREEN). Verified via `pytest tests/test_flexible_auth.py tests/test_turnos_router.py` → 42 passed.

## 4. Wire the flexible dependency into `turnos.py`

- [x] 4.1 In `backend/app/routers/turnos.py`, import `FlexibleProfesionalDep` from `app.dependencies` alongside the existing `DbDep, CurrentProfesionalDep` import (replaced `CurrentProfesionalDep` import with `FlexibleProfesionalDep`, since no endpoint in this file uses the JWT-only dep anymore).
- [x] 4.2 Replace `profesional: CurrentProfesionalDep` with `profesional: FlexibleProfesionalDep` on all 7 endpoints — re-verified by grep: exactly 7 occurrences found and replaced (`get_turnos_disponibles`, `create_turno`, `confirmar_turno_endpoint`, `cancelar_turno_endpoint`, `reprogramar_turno_endpoint`, `completar_turno_endpoint`, `confirmar_asistencia_endpoint`). Every endpoint body, `response_model`, and business logic left untouched.
- [x] 4.3 Run `pytest backend/tests/test_flexible_auth.py backend/tests/test_turnos_router.py` and confirm all pass — 42 passed.

## 5. Triangulation — tenant isolation and cross-scheme scoping

- [x] 5.1 Add a test asserting that a `/turnos/*` call authenticated via `X-API-Key` for professional A only returns/mutates turnos scoped to `profesional.id == A.id` (e.g. `GET /turnos/disponibles` or the professional's own turno list), matching existing JWT-scoping behavior (spec: "Professional-scoped queries use the resolved id"). `test_api_key_scoping_identico_a_jwt_scoping`.
- [x] 5.2 Add a regression test confirming `get_profesional_by_api_key` (used directly by `recordatorios.py`) is untouched: still does a plaintext equality comparison against `Profesional.api_key`, still returns its own distinct `"API Key inválida"` message on failure. `test_recordatorios_key_invalida_mantiene_mensaje_propio`.
- [x] 5.3 Run the full `test_flexible_auth.py` + `test_recordatorio_router.py` + `test_turnos_router.py` suite together and confirm green. Result: 50 passed.

## 6. Refactor

- [x] 6.1 Review `get_current_profesional_flexible` for duplication against `get_current_profesional` / `get_profesional_by_api_key` (e.g. the JWT-decode block, the active-professional SELECT) and extract a small private helper only if it does not obscure the precedence logic or the single-exception guarantee from task 3.4. Extracted `_flexible_lookup_por_api_key` and `_flexible_lookup_por_bearer_jwt` (private, scoped only to the new dependency — `get_current_profesional` and `get_profesional_by_api_key` left untouched per hard constraint). Result: the shared 401 is now built at exactly one call site.
- [x] 6.2 Re-run `pytest backend/tests/test_flexible_auth.py backend/tests/test_turnos_router.py backend/tests/test_recordatorio_router.py backend/tests/test_pacientes_router.py backend/tests/test_lista_espera_api.py` after any refactor step — must stay green after every edit. Result: 71 passed.

## 7. Full regression pass

- [x] 7.1 Run the complete backend suite (`pytest backend/tests`) and diff the pass count against the task 1.2 baseline — no new failures, no skipped/xfailed tests introduced. Baseline: 468 passed / 5 failed / 1 skipped / 1 xfailed / 3 xpassed. Final: 479 passed / 5 failed (same 5, unrelated) / 1 skipped / 1 xfailed / 3 xpassed. Delta: +11 passed (exactly `test_flexible_auth.py`'s new tests), zero regressions.
- [x] 7.2 Manually confirm (by reading the final `turnos.py` and `dependencies.py`) that `pacientes.py`, `lista_espera.py`, `profesional.py`, `recordatorios.py`, and `webhooks.py` were not modified — scope stays limited to the two files named in the proposal/design. Confirmed via `git status --porcelain`: only `backend/app/dependencies.py` and `backend/app/routers/turnos.py` modified, plus the new `backend/tests/test_flexible_auth.py`.

## 8. n8n workflow rebind (out of backend test scope — manual/config change)

- [x] 8.1 In `n8n-workflows/sub-flujo-crear-turno.json`, `sub-flujo-cancelar-turno.json`, and `sub-flujo-reprogramar-turno.json`, rebind the HTTP node(s) calling `/turnos/*` from the `httpHeaderAuth` credential bound to `Authorization` (id `Aaw5tL1OiWSNc2Eg`) to the `X-API-Key` credential (mirror `flujo-recordatorio` / `flujo-lista-espera`, credential id `profesional-api-key`). Rebound the 2 HTTP nodes in `sub-flujo-crear-turno.json` (`Aaw5tL1OiWSNc2Eg` / "Authorization" → `profesional-api-key` / "Profesional API Key"). `sub-flujo-cancelar-turno.json` (1 node) and `sub-flujo-reprogramar-turno.json` (2 nodes) already referenced `profesional-api-key` — no edit needed. Verified by grep: zero remaining occurrences of `Aaw5tL1OiWSNc2Eg` in `n8n-workflows/` outside `archive/`.
- [x] 8.2 Confirm no node inlines the API key as a plain header parameter (must stay a masked n8n credential, per design's residual-risk note) — inspect the node JSON for the credential reference, not a literal value. Confirmed: all 5 `/turnos/*` HTTP nodes across the 3 sub-flows use `"genericAuthType": "httpHeaderAuth"` + a `credentials.httpHeaderAuth` reference to id `profesional-api-key`. No literal key value and no `X-API-Key` entry under `headerParameters` in any node.
- [~] 8.3 Manually trigger each rebound sub-flow once against a non-production/staging backend (or local run) and confirm a 200 response with `X-API-Key` auth before considering the workflow rebind complete. **PARTIAL — 1 of 3 verified. Not a defect of this change; blocked by a domain precondition.**
  - `sub-flujo-crear-turno` — **VERIFIED**. Six successful executions against the local backend on 2026-07-31 (n8n executions 231, 234, 236, 238, 240, 243), all authenticating with `X-API-Key`. Backend cross-check: `GET /turnos/disponibles` returns 401 with no auth and 200 with a valid `X-API-Key`.
  - `sub-flujo-cancelar-turno`, `sub-flujo-reprogramar-turno` — **NOT VERIFIABLE at this time.** Both endpoints require the target turno to be in state `CONFIRMADO` (`turno_service.py:418` for cancel, `:480` for reschedule). The system currently produces zero `CONFIRMADO` turnos: the Telegram creation flow calls `POST /turnos` (leaving the turno in `RESERVADO_TEMPORAL`) but never calls `PUT /turnos/{id}/confirmar`, because `ConfirmarTurnoRequest` requires `nombre`/`apellido`/`dni`/`telefono` and the bot never collects them. The temporary reservation then expires after `reserva_temporal_minutos = 10`. With no confirmed turno in existence, there is nothing to cancel or reschedule — the smoke test as written is unsatisfiable until the creation flow is completed.
  - Static verification performed in lieu of runtime: all 5 `/turnos/*` HTTP nodes across the 3 sub-flows carry `authentication: "genericCredentialType"` + `genericAuthType: "httpHeaderAuth"` + a credential reference to `Profesional API Key`, i.e. structurally identical to the two nodes proven working in `sub-flujo-crear-turno`. Note that the cancel/reschedule nodes had to be repaired first (commit `066a98a`): they declared `authentication` as a nested object, which `HttpRequestV3` does not recognize, silently dropping the auth header. Those repairs have not been exercised at runtime.
  - **Follow-up**: re-run this task for cancel and reschedule once the change that completes the creation flow lands and the system can produce a `CONFIRMADO` turno.
