# Tasks — c-27-confirmacion-turno-telegram

> Strict TDD is active for backend work: RED before GREEN, triangulate, refactor green.
> Groups 1–5 are backend; groups 6–8 are n8n; group 9 closes the c-26 follow-up.
> Backend ships before the workflow (design § Migration Plan): a workflow calling a
> lookup that does not exist fails on every new-patient booking.

## 1. Spike and baseline

- [x] 1.1 Resolve **OQ-1**: build a throwaway n8n workflow against the live instance using `Telegram - Send and Wait for Response` with a free-text response, and record whether the user answers in-chat or is sent to a hosted form URL. Write the finding into `design.md` under D3. **Finding: hosted form, confirmed via a real triggered execution (waiting state reached) plus direct inspection of the installed `n8n-nodes-base` source (`utils/sendAndWait/utils.js`) — `responseType: "freeText"` sends a button linking to a signed resume URL that renders an n8n-hosted textarea form; there is no in-chat-reply code path. Throwaway workflow deleted after the spike; instance back to its original 6 workflows.**
- [x] 1.2 If 1.1 shows in-chat free-text replies work, STOP and revise D3 before writing any capture node — holding state inside a live execution removes the `staticData` machinery that groups 6–7 assume. Otherwise record D3 as confirmed and continue. **Not triggered — 1.1 confirmed the hosted-form behavior, so D3's `staticData` approach stands unchanged. Continuing to 1.3 and groups 2–3.**
- [x] 1.3 Capture the backend test baseline (`pytest backend/tests`) and record the pass/fail counts, so group 5's regression check has something to diff against. **Baseline (`cd backend && pytest --tb=no -q`): `5 failed, 480 passed, 4 xfailed, 2 warnings in 774.51s`. All 5 failures are pre-existing and unrelated to this change's scope (`test_notificacion_service.py::TestEnviarRecordatorioTelegram::test_envio_exitoso_retorna_true`, and 4 in `test_telegram_service.py` — `TestMessageFormatting` × 3, `TestListaEsperaTelegram` × 1 — all mocking/caplog-related, not touching `pacientes.py`/`paciente_service.py`). Reported as pre-existing, not fixed.**

## 2. Patient lookup by DNI — RED

- [x] 2.1 Write a failing test: lookup for a DNI belonging to a patient of the authenticated professional returns `200` with `id`, `nombre`, `apellido`, `dni`, `telefono`. **`tests/test_pacientes_router.py::TestPacienteBuscarPorDni::test_buscar_dni_existente_devuelve_200_con_datos` + service-level `tests/test_paciente_service.py::TestBuscarPacientePorDni::test_buscar_paciente_por_dni_existente`.**
- [x] 2.2 Write a failing test: lookup for an unregistered DNI returns `404`. **`test_buscar_dni_no_registrado_devuelve_404` + `test_buscar_paciente_por_dni_no_registrado`.**
- [x] 2.3 Write a failing test: lookup with a missing or empty DNI returns `422` and performs no query. **`test_buscar_sin_dni_devuelve_422_sin_query` (monkeypatches the service call and asserts it was never invoked) + `test_buscar_dni_vacio_devuelve_422`.**
- [x] 2.4 Write a failing test: when professionals A and B both have a patient with the same DNI, A's lookup returns A's patient and never B's. **`test_buscar_mismo_dni_dos_profesionales_devuelve_el_propio` + `test_buscar_paciente_por_dni_aislamiento_entre_profesionales`.**
- [x] 2.5 Write a failing test: a DNI existing only under professional A returns `404` for professional B. **`test_buscar_dni_existe_solo_bajo_otro_profesional_devuelve_404` + `test_buscar_paciente_por_dni_existe_solo_bajo_otro_profesional`.**

## 3. Patient lookup by DNI — GREEN

- [x] 3.1 Add the lookup to `paciente_service.py`, querying on `(profesional_id, dni)`. **`buscar_paciente_por_dni(db_session, profesional_id, dni) -> Optional[PacienteBusqueda]`.**
- [x] 3.2 Add the response schema to `schemas/paciente.py`. **`PacienteBusqueda` — `id`, `nombre`, `apellido`, `dni`, `telefono` (no `creado_en`, per spec's exact field list).**
- [x] 3.3 Add `GET /pacientes/buscar` to `pacientes.py`, **declared before `GET /pacientes/{paciente_id}`** — FastAPI matches in declaration order and would otherwise parse `buscar` as a `paciente_id` and return `422` (design § D1). **Uses `CurrentProfesionalDep` (JWT-only) — the dual-auth widening is group 4, out of this batch's scope.**
- [x] 3.4 Run the group 2 tests to green. **`pytest tests/test_paciente_service.py tests/test_pacientes_router.py -k Buscar` → 11 passed. Full `test_paciente_service.py` + `test_pacientes_router.py` + `test_paciente.py` → 39 passed, 0 regressions.**
- [x] 3.5 Add a test asserting the route ordering itself: a request to `/pacientes/buscar?dni=...` is not captured by the `{paciente_id}` route. This guards against a later reorder silently breaking the endpoint. **`test_ruta_buscar_no_es_capturada_por_ruta_paciente_id`.**

## 4. Flexible auth on `pacientes.py` — CRITICAL governance

> Auth-surface change: widens what the n8n credential reaches from `/turnos/*` to
> `/pacientes/*`. Per project governance this needs explicit human approval before
> the code is written, and one test per endpoint rather than a single dependency test.

- [x] 4.1 Confirm explicit approval to proceed with the auth widening before writing any code in this group. **Approval granted explicitly by the user in conversation on 2026-07-31, scoped to group 4 exactly as written (widening `pacientes.py` auth from JWT-only to X-API-Key OR Bearer JWT via `FlexibleProfesionalDep`) — no wider auth surface.**
- [x] 4.2 Write failing tests, one per endpoint (`POST /pacientes`, `GET /pacientes`, `GET /pacientes/{id}`, `GET /pacientes/{id}/turnos`, `GET /pacientes/buscar`): a valid `X-API-Key` is accepted. **`tests/test_pacientes_flexible_auth.py::TestApiKeyAceptadoEnPacientes` — 5 tests, one per endpoint. Confirmed RED (403, HTTPBearer's `auto_error=True` rejecting the missing `Authorization` header) before 4.7.**
- [x] 4.3 Write failing tests, same five endpoints: an existing `Authorization: Bearer` JWT still works — regression guard, the JWT path must not be traded away for the key path. **`TestJwtSigueFuncionandoEnPacientes` — 5 tests. Already GREEN pre-4.7 (JWT path was never broken) — a safety-net regression guard, not new behavior; re-confirmed green post-4.7.**
- [x] 4.4 Write a failing test: no credentials returns `401` with a generic message that does not disclose which schemes are accepted. **`TestSinCredencialesPacientes` — 2 tests (bare no-headers 401, and invalid X-API-Key alone gets the byte-identical generic body — no scheme leak). Confirmed RED (403) before 4.7.**
- [x] 4.5 Write a failing test: an `X-API-Key` belonging to an inactive professional returns `401`. **`TestApiKeyInactivaPacientes::test_api_key_profesional_inactivo_401`. Confirmed RED (403) before 4.7.**
- [x] 4.6 Write a failing test: a professional authenticated by API key sees only their own patients — cross-professional isolation. **`TestAislamientoPorApiKeyPacientes` — 2 tests (`GET /pacientes` and `GET /pacientes/buscar`). Confirmed RED (403) before 4.7.** Also updated the stale c-26 regression test `test_flexible_auth.py::TestFlexibleAuthTurnosDisponibles` (was `test_endpoint_no_turno_no_afectado_por_x_api_key`, asserted `/pacientes` stayed JWT-only) to `test_endpoint_pacientes_tambien_acepta_x_api_key`, documenting the deliberate widening instead of leaving a now-false assertion in the suite.
- [x] 4.7 Swap `CurrentProfesionalDep` for `FlexibleProfesionalDep` across all endpoints in `pacientes.py`, reusing `get_current_profesional_flexible` unchanged (design § D2). **`backend/app/routers/pacientes.py` — import swapped, all 5 endpoint signatures swapped (mechanical `sed`-style replace, `get_current_profesional_flexible` itself untouched).**
- [x] 4.8 Run groups 2 and 4 to green. **`pytest tests/test_pacientes_flexible_auth.py tests/test_flexible_auth.py tests/test_pacientes_router.py tests/test_paciente_service.py tests/test_paciente.py` → 65 passed, 0 failures.**

## 5. Backend regression

- [x] 5.1 Run the full backend suite and diff against the 1.3 baseline — no new failures, nothing newly skipped. **`cd backend && pytest --tb=no -q` → `5 failed, 506 passed, 4 xfailed, 2 warnings in 631.28s`. Same 5 pre-existing failures as the 1.3 baseline, byte-identical test IDs (`test_notificacion_service.py::TestEnviarRecordatorioTelegram::test_envio_exitoso_retorna_true` + 4 in `test_telegram_service.py`). `4 xfailed` matches baseline exactly (no `test_concurrency.py` drift this run). Passed count is baseline 491 + 15 new group-4 tests (`test_pacientes_flexible_auth.py`) = 506, confirmed exact. Zero new failures, zero newly skipped.**
- [x] 5.2 Confirm by inspection that `turno_service.confirmar_turno`, `_upsert_destinatarios_confirmacion` and `ConfirmarTurnoRequest` were not modified; this change relies on them as-is. **`git diff --stat backend/app/services/turno_service.py backend/app/schemas/turno.py` → empty output, byte-identical to the committed baseline. `confirmar_turno` at `turno_service.py:159`, `_upsert_destinatarios_confirmacion` at `turno_service.py:256`, `ConfirmarTurnoRequest` in `schemas/turno.py` — none touched by groups 1-5.**
- [x] 5.3 Verify the lookup end-to-end against the running stack with a real `X-API-Key`: a known DNI returns `200`, an unknown one `404`. **Backend actually reachable at `http://localhost:18000` (docker-compose maps container port 8000 → host 18000; host port 8000 is occupied by an unrelated project's API — noted as a risk below). Used profesional id 2's real `api_key` from the dev DB: `GET /pacientes/buscar?dni=12345678` (paciente id 3, belongs to profesional 2) → `200` with full patient data; `GET /pacientes/buscar?dni=00000000` (no such patient) → `404 {"detail":"Paciente no encontrado"}`. Also spot-checked the generic-401 contract live: no headers and a bogus `X-API-Key` both → `401 {"detail":"Credenciales inválidas"}`, matching the test suite.**

## 6. Orquestador — stateful routing

- [x] 6.1 In `orquestador.json`, inside the `Normalizar Comando` jsCode, read `gate_captura_<chatId>` from the **orquestador's own** `$getWorkflowStaticData('global')`, treating a key past its `expira_en` as absent. The sub-flujo's store is not readable from here — `staticData` is per-workflow (design § D3). **`leerGateCaptura()`/`limpiarGateCaptura()` helpers added to `Normalizar Comando`.**
- [x] 6.2 When the gate is set, set `comando = 'crear'` and `payload.respuesta_captura = textRaw`, dispatching through the **existing** `crear` output — no new `Switch Comando` output. This check runs **above** the `HH:MM` / `1–20` / `DD/MM/AAAA` cascade, suspending shape matching while a capture is pending, so a mistyped DNI of `15` is an invalid DNI and not slot number 15 (design § D4). **Verified empirically (`node -e` simulation): text `"15"` with an unexpired gate yields `payload.respuesta_captura: "15"`, not `slot_numero`.**
- [x] 6.2a Make `Execute Crear Turno` non-terminal: add `Code - Sync Captura` (reads `chat_id` via `$('Normalizar Comando').first().json.chat_id`; writes or deletes `gate_captura_<chatId>` from the sub-workflow's returned `captura_pendiente` / `expira_en`; outputs `{chat_id, mostrar_ayuda}`), then `IF - Mostrar Ayuda` routing to the **existing** `Telegram - Mensaje de Ayuda` node. The node already has `waitForSubWorkflow: true` and no outgoing connection — it waits for the response and discards it. **Both nodes added; `Execute Crear Turno` → `Code - Sync Captura` → `IF - Mostrar Ayuda` (true → `Telegram - Mensaje de Ayuda`, false → terminal).**
- [x] 6.3 Implement the escape hatch: a recognised command (`/cancelar`, `/reprogramar`, a new `cmd:crear`) wins over a pending capture and **clears the gate**, so an abandoned booking cannot trap the user. Clearing the gate alone is sufficient — the payload in the sub-flujo is unreachable without it, and is disposed of by the existing `freshStart`, by the next slot selection overwriting the key, and by its TTL. **Verified: `/cancelar` with a pending gate clears `gate_captura_<chatId>` and routes to `comando: 'cancelar'`.**
- [x] 6.4 Leave the existing routing untouched when no capture is pending — this path must behave exactly as it does today. Verify against the `HH:MM`, `1–20` and `DD/MM/AAAA` paths specifically, not only the help path: 6.2a rewires the busiest branch in the orquestador. **Verified via simulation: `10:30` → `payload.slot`, `5` → `payload.slot_numero`, `15/07/2026` → `payload.fecha_raw`, all byte-identical to pre-c-27 output shape. Also verified an expired gate (`expira_en` in the past) is treated as absent and falls through to the same cascade.**
- [x] 6.5 Verify precedence explicitly: with a capture pending, `/cancelar` clears the gate and reaches `Execute Cancelar Turno`; `15` is treated as a DNI answer, not `slot_numero`. **Verified in 6.2/6.3's simulations above (`comando: 'cancelar'` for `/cancelar`, `respuesta_captura: '15'` for `15`).**

## 7. Capture flow in `sub-flujo-crear-turno`

- [x] 7.0 Add `Code - Retorno Estado`: a read-only fan-in node wired from **every** terminal branch, returning `{chat_id, captura_pendiente, expira_en, mostrar_ayuda}` read from its own `captura_<chatId>`. `chat_id` via `$('Code - Decidir Paso').first().json.chat_id`. Build this first, so every branch added later is wired into it as it is built. **All 13 terminal Telegram nodes (6 pre-existing + 7 new) now connect into `Code - Retorno Estado`; it is the sub-flujo's only terminal node.**
- [x] 7.0a Extend `Code - Decidir Paso`: when `payload.respuesta_captura` is present and `captura_<chatId>` is live, emit `accion = 'captura'` and skip the fecha/slot logic. Add a `captura` output to `Switch Accion`. **Vigencia check moved into `Code - Procesar Captura` (single owner of the state machine); `Code - Decidir Paso` short-circuits on presence of `respuesta_captura` alone. `Switch Accion` gained rule 6 (`captura`).**
- [x] 7.1 After `HTTP - POST Crear Turno`, write `captura_<chatId>` = `{turno_id, paso: 'dni', datos: {}, expira_en}` **only when `exito` is true**, and ask for the DNI. Set `neverError` + `fullResponse` on `HTTP - POST Crear Turno`: `Code - Extraer Turno ID` already tests `statusCode >= 400`, but without those options a 4xx throws before reaching it and `statusCode` never arrives — that check is dead code today. Rename and retext `Telegram - Turno Creado` → `Telegram - Pedir DNI`, removing the premature "reserva creada / te enviaremos un recordatorio" message. **`expira_en` = `RESERVA_TEMPORAL_MINUTOS` (default 10) + 1 min grace, per OQ-4 (backend does not expose expiry in `POST /turnos`'s response — confirmed by reading `TurnoResponse` schema).**
- [x] 7.2 Validate the DNI reply in `Code - Procesar Captura`; on an implausible value keep `turno_id` and `paso` intact and route to `Telegram - DNI Invalido` via `Switch - Paso Captura`, so the reservation is not abandoned. **DNI regex `/^\d{7,8}$/`. Verified via simulation: invalid DNI keeps `paso: 'dni'` and `turno_id` in the store untouched.**
- [x] 7.3 Call `GET /pacientes/buscar?dni=` from `HTTP - GET Buscar Paciente` with `neverError: true` + `fullResponse: true`, branching on `statusCode` 200 vs 404 in `Code - Evaluar Paciente`. Read `chat_id` via `$('Code - Decidir Paso').first().json.chat_id`. Without `neverError` the `404` throws and aborts the branch.
- [x] 7.4 Existing-patient branch: copy `nombre`, `apellido` and `telefono` into `captura_<chatId>.datos`, set `paso = 'confirmar'`, and call `PUT /turnos/{id}/confirmar` with them plus `dni` and the `telegram_chat_id`. Ask nothing further. **`IF - Paciente Encontrado` true branch goes straight to `HTTP - PUT Confirmar Turno`, no further questions.**
- [x] 7.5 New-patient branch: ask for name and surname in one message, then phone and optional email in one message (design § D5). `paso` transitions `dni → nombre → telefono → confirmar`, each answer written into `captura_<chatId>.datos` before the next question is sent. **`Telegram - Pedir Nombre` then `Telegram - Pedir Telefono`; `Code - Procesar Captura` advances `paso` on each answer.**
- [x] 7.6 New-patient branch: call `PUT /turnos/{id}/confirmar` from `HTTP - PUT Confirmar Turno` with the captured data, including `email` when supplied. Set `neverError: true` + `fullResponse: true` — without them 7.9 and 7.10 are unreachable.
- [x] 7.7 On **every** terminal outcome — confirmed, expired, or rejected — delete `captura_<chatId>`, `ctx_<chatId>`, `contador_<chatId>` and `fecha_<chatId>` together. This also **fixes the pre-existing `ctx_` leak** (never cleared after a successful creation). The orquestador's gate needs no separate cleanup: `Code - Retorno Estado` reports `captura_pendiente: false` and 6.2a clears it on the same round trip. **Implemented in `Code - Evaluar Confirmacion`; verified via simulation for all 3 outcomes (confirmado/expirado/paciente_turno_activo) that all 4 keys are deleted.**
- [x] 7.8 Report success with patient name, date and time. The message announces the Telegram reminder only and must **not** promise an email reminder (spec, "Email delivery is not promised" — no mail sender exists). **`Telegram - Turno Confirmado` text reviewed — mentions Telegram reminder only.**
- [x] 7.9 Handle `409 TurnoExpiradoError`: tell the user the reservation lapsed and invite them to book again. Reachable only because of 7.6's `neverError` — assert the branch runs rather than the node throwing. **Distinguished from 7.10 by `body.detail` containing "expir" (backend message: "La reserva temporal ha expirado"). Verified via simulation.**
- [x] 7.10 Handle `409 PacienteConTurnoActivoError`: report that the patient already holds an active turno. Same reachability note as 7.9. **Distinguished by `body.detail` containing "turno activo" (backend message: "El paciente ya tiene un turno activo"). Verified via simulation.**
- [x] 7.11 Confirm every new HTTP node uses `authentication: "genericCredentialType"` + `genericAuthType: "httpHeaderAuth"` as sibling string props, and every expression parameter starts with `=` (`n8n-workflows/README.md` §2 and §3 — both have already broken this repo). Additionally: a TTL (`expira_en`) is written on every capture key; every new Code node reads `chat_id` via the named-node pattern; and every new Telegram node uses the `telegramApi` credential named `Telegram account`, **not** the raw `$env.TELEGRAM_BOT_TOKEN` HTTP call that `Telegram - Pedir Slot` uses. **All verified programmatically (see apply summary): 3/3 new HTTP nodes have `neverError`+`fullResponse`; all Telegram nodes reference credential `Telegram account`; all HTTP nodes reference `Profesional API Key`; no bare `{{ }}` without a leading `=` in either file.**

**Note for group 8 (not run in this batch):** both JSON files were validated for JSON-parse validity, full node-graph reachability (every node reachable from its trigger, no dangling connection references, switch/IF output-array counts matching rule counts), and jsCode syntax validity via `node -e` scripts. The actual HTTP calls, Telegram API round-trips, and live credential resolution were **not** exercised — that is exactly what group 8 (push to the live instance + E2E) still needs to verify. Nothing was pushed to the live n8n instance; it remains at its pre-existing 6 workflows.

## 8. n8n end-to-end verification

- [x] 8.1 Push the updated workflows to the live instance and confirm the credential resolved by name rather than landing orphaned. **Pushed 2026-08-05 via `n8n-cli workflows update --file` (backups: orquestador v6, sub-flujo-crear-turno v8). The n8n public API rejects the repo's full workflow shape (`request/body must NOT have additional properties`), so the payload must be reduced to `name`/`nodes`/`connections`/`settings` — repo files keep their full shape. Verified live: both `active: true`, 9 and 31 nodes, zero orphaned credentials; `Telegram account` → `TZgUFY41hdHCU9fu`, `Profesional API Key` → `T9DkbhELoxyryvzg`, both resolved by name. Note: `n8n-cli`'s local validator reports a false `UNKNOWN_NODE_TYPE` for `n8n-nodes-base.telegram`; a known-good untouched workflow fails identically, so `--skip-validation` is required.**
- [ ] 8.2 Book as a **new** patient end-to-end: three questions, turno reaches `CONFIRMADO`, `ReservaTemporal` row gone, `paciente_id` set.
- [ ] 8.3 Book as a **returning** patient end-to-end: one question, no re-typing, turno reaches `CONFIRMADO`.
- [ ] 8.4 Confirm the slot disappears from a subsequent availability query for the same date.
- [ ] 8.5 Verify the recipients: a `TELEGRAM` row always; an `EMAIL` row when an email was supplied; both coexisting on one turno.
- [ ] 8.6 Force the expiry path — start a booking, wait out the reservation window, then answer — and confirm the user is told the reservation lapsed.
- [ ] 8.7 Abandon a booking mid-question, then issue `/cancelar`, and confirm the escape hatch clears the pending capture instead of swallowing the command.

## 9. Close the c-26 follow-up

- [ ] 9.1 With a `CONFIRMADO` turno now reachable, run the smoke test that c-26 task 8.3 could not: trigger `sub-flujo-cancelar-turno` once and confirm a `200` with `X-API-Key` auth.
- [ ] 9.2 Same for `sub-flujo-reprogramar-turno`. Note these nodes were repaired in commit `066a98a` and have never been exercised at runtime — expect defects.
- [ ] 9.3 Update c-26's `tasks.md` 8.3 from partial to complete, recording the evidence, and note in this change that the follow-up is discharged.

## 10. Documentation

- [x] 10.1 Correct `n8n-workflows/README.md`: the workflow table marks `sub-flujo-crear-turno` as "✅ Completo" and describes a `CSV → confirmar` step that was never built. Both are now wrong in a new way. **Row corrected: description `Wizard de crear turno (fecha → hora → CSV → confirmar)` → `Crea reserva temporal (fecha → hora); captura y confirmación en progreso`; status `✅ Completo` → `🔄 Parcial (C-27)`.**
- [ ] 10.2 Document the capture flow and its `staticData` keys next to the existing conventions, so the next person does not rediscover the state model by reading nodes.
- [x] 10.3 Record the email limitation where it will be found: recipients are stored on channel `EMAIL` and nothing delivers them until a mail sender exists. **Warning note added to the "Dos motores de recordatorio" section of `n8n-workflows/README.md`, where reminders are already documented — not appended at the end of the file.**
